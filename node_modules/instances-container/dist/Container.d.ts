import { InstanceOption } from './definitions';
export declare class Container {
    instances: any;
    constructor(options?: InstanceOption[]);
    private static verifyOptions;
    private static verifyParameterOption;
    private static verifyDestructuringDependencies;
    private static verifyParameterDependencies;
    private addInstances;
    getInstance(key: string): any;
    destroyInstance(key: string): void;
    destroyAllInstances(): void;
    register(options: InstanceOption[] | InstanceOption): void;
    private buildParameters;
}
